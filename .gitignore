# =========================================
# Android / Gradle — Production-Ready .gitignore (Final, Submodule Safe)
# -----------------------------------------
# Notes:
# - Keep Gradle Wrapper in VCS: gradlew, gradlew.bat, gradle/wrapper/** MUST be committed.
# - Supports multi-module projects (app/, :feature, :nativelib, etc.).
# - Prebuilt JNI libs under src/**/jniLibs/** SHOULD be committed; build artifacts should not.
# - Submodules (e.g., whisper_core) are tracked only by reference (.gitmodules).
# =========================================

# -----------------------------------------
# Gradle caches & build outputs
# -----------------------------------------
.gradle/
**/build/
**/.cxx/
**/.externalNativeBuild/
.kotlinc/
.kotlin/

# Keep Gradle Wrapper tracked
!gradlew
!gradlew.bat
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# Local environment / secrets
local.properties
gradle.properties
!gradle.public.properties
!gradle-example.properties

# -----------------------------------------
# Android build artifacts & packaging
# -----------------------------------------
*.apk
*.aab
*.ap_
*.dex
output.json
output-metadata.json
**/outputs/mapping/
**/lint-results*
**/reports/
**/test-results/
**/androidTest-results/

# -----------------------------------------
# Logs & temporary files
# -----------------------------------------
*.log
*.temp
*.tmp
*.orig
*.rej
*.bak
*.swp
*.swo
*.patch
*.toc
*.aux

# Crash / heap / trace artifacts
*.hprof
*.ctest
*.trace
*.gpuprofile
*.coredump
core
core.*

# -----------------------------------------
# IDE / Tooling
# -----------------------------------------
.idea/
*.iml
*.ipr
*.iws
.vscode/
.fleet/
#!.idea/codeStyles/
#!.idea/runConfigurations/

# -----------------------------------------
# OS / System files
# -----------------------------------------
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
*~

# -----------------------------------------
# Android-specific folders
# -----------------------------------------
captures/
google-services.json
!google-services.example.json
**/crashlytics-build.properties
*.jks
*.keystore
keystore.properties
signing.properties

# Environment / secrets
.env
.env.*
!.env.example
secrets.*
*.pem
*.p12
*.cer
*.key

# -----------------------------------------
# NDK / C++ Native (CMake / ndk-build) artifacts
# -----------------------------------------
**/obj/
**/libs/
!**/src/**/jniLibs/
!**/src/**/jniLibs/**
**/cmake-build*/
**/CMakeFiles/
**/CMakeCache.txt
**/cmake_install.cmake
**/Makefile
**/build.ninja
**/.ninja_deps
**/.ninja_log
**/rules.ninja

# -----------------------------------------
# Kotlin / Annotation processors
# -----------------------------------------
.kapt/
.ksp/

# -----------------------------------------
# Testing / Benchmarking
# -----------------------------------------
**/benchmark/**/reports/
**/benchmark/**/results/
**/benchmark/**/outputs/
**/profiling/**

# -----------------------------------------
# Module / Subproject-specific (example: nativelib)
# -----------------------------------------
nativelib/.cxx/
nativelib/build/
nativelib/obj/
!nativelib/src/
!nativelib/src/**

# -----------------------------------------
# Submodules (REFERENCE ONLY)
# -----------------------------------------
# Always track submodule references, not contents.
!.gitmodules
# Submodule placeholder — contents ignored
nativelib/whisper_core/*
!nativelib/whisper_core/.git

# (Add more submodules below if needed)
# e.g.
# third_party/ggml/*
# !third_party/ggml/.git

# -----------------------------------------
# Optional toolchains (uncomment if used)
# -----------------------------------------
#.ktlint/
#.detekt/
#.scannerwork/
#node_modules/
#pnpm-lock.yaml
#package-lock.json
#yarn.lock
#dist/
