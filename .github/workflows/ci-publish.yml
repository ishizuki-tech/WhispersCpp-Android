# ============================================================
# ✅ WhispersCpp-Android — CI Publish (v9.9.2 AutoBranch + SafeHTML Final)
# ------------------------------------------------------------
# • Triggered after Build workflow success
# • No manual branch input (auto head_branch)
# • Safe HTML generation (no syntax errors)
# • Wiki + Pages auto-sync (Glass UI)
# ============================================================

name: Android CI Publish (Meta + Pages + Wiki)

on:
  workflow_dispatch:
    inputs:
      artifact_name:
        description: "Artifact name uploaded by build workflow"
        default: "release-artifacts"
  workflow_run:
    workflows: ["Android CI Build (Detect + Review + Build + Release)"]
    types: [completed]

permissions:
  contents: write
  pages: write

defaults:
  run:
    shell: bash

env:
  EXPECTED_WORKFLOW_NAME: "Android CI Build (Detect + Review + Build + Release)"
  DEFAULT_ARTIFACT_NAME: "release-artifacts"

jobs:
  meta_publish:
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') ||
      (github.event_name == 'workflow_dispatch')

    steps:
      # ------------------------------------------------------------
      # 🧩 Checkout (auto branch from Use workflow from)
      # ------------------------------------------------------------
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_branch || github.ref_name }}

      # ------------------------------------------------------------
      # 🛠️ Dependencies
      # ------------------------------------------------------------
      - name: 🛠️ Install dependencies (jq/unzip/curl)
        run: |
          set -euxo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y jq unzip curl

      # ------------------------------------------------------------
      # 🔎 Resolve build run ID (auto)
      # ------------------------------------------------------------
      - name: 🔎 Resolve build run ID
        id: resolve_run
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            RUN_ID="${{ github.event.workflow_run.id }}"
            BRANCH="${{ github.event.workflow_run.head_branch }}"
            echo "✅ Linked build run: $RUN_ID (branch=${BRANCH})"
            echo "run_id=$RUN_ID" >> "$GITHUB_OUTPUT"
            echo "branch=$BRANCH" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "🧭 Manual trigger — searching latest successful build..."
          REPO="${{ github.repository }}"
          WF_EXPECTED="${{ env.EXPECTED_WORKFLOW_NAME }}"
          RUN_ID=$(curl -s -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://api.github.com/repos/${REPO}/actions/runs?status=success&per_page=10" \
            | jq -r --arg name "${WF_EXPECTED}" '.workflow_runs[] | select(.name==$name) | .id' | head -n1)
          [ -z "$RUN_ID" ] && { echo "::error::No successful build run found."; exit 1; }
          echo "run_id=$RUN_ID" >> "$GITHUB_OUTPUT"

      # ------------------------------------------------------------
      # 🧠 Extract tag from meta/models.json
      # ------------------------------------------------------------
      - name: 🧠 Extract tag from meta/models.json
        id: tag_from_meta
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          mkdir -p _meta
          REPO="${{ github.repository }}"
          RUN_ID="${{ steps.resolve_run.outputs.run_id }}"
          echo "🔍 Fetching meta artifact list for run $RUN_ID..."
          curl -s -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://api.github.com/repos/${REPO}/actions/runs/${RUN_ID}/artifacts" > _meta/list.json

          URL=$(jq -r '.artifacts[] | select(.name=="meta") | .archive_download_url' _meta/list.json | head -n1)
          if [ -z "$URL" ] || [ "$URL" = "null" ]; then
            echo "::notice::No meta artifact found."
            echo "version_tag=unreleased" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          curl -sL -H "Authorization: Bearer ${GH_TOKEN}" "$URL" -o _meta/meta.zip
          unzip -o _meta/meta.zip -d _meta >/dev/null || true
          TAG=$(jq -r '.version_tag // "unreleased"' _meta/models.json)
          echo "🪶 Tag: $TAG"
          echo "version_tag=$TAG" >> "$GITHUB_OUTPUT"

      # ------------------------------------------------------------
      # 📦 Retrieve build artifacts
      # ------------------------------------------------------------
      - name: 📦 Retrieve build artifacts
        id: fetch
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          mkdir -p artifacts
          REPO="${{ github.repository }}"
          RUN_ID="${{ steps.resolve_run.outputs.run_id }}"
          NAME_PRI="${{ inputs.artifact_name || env.DEFAULT_ARTIFACT_NAME }}"
          echo "🔎 Searching artifacts for run $RUN_ID..."
          curl -s -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://api.github.com/repos/${REPO}/actions/runs/${RUN_ID}/artifacts" > artifacts_list.json

          URL=$(jq -r --arg n "$NAME_PRI" '.artifacts[] | select(.name==$n) | .archive_download_url' artifacts_list.json | head -n1)
          [ -z "$URL" ] && URL=$(jq -r '.artifacts[0].archive_download_url' artifacts_list.json)
          if [ -z "$URL" ] || [ "$URL" = "null" ]; then
            echo "::warning::No artifact URL found — skipping pages/wiki but keeping job green."
            exit 0
          fi

          curl -sL -H "Authorization: Bearer ${GH_TOKEN}" "$URL" -o artifacts.zip
          unzip -o artifacts.zip -d artifacts >/dev/null || true
          echo "📂 Artifact files:"; ls -lh artifacts || true

      # ------------------------------------------------------------
      # 🌈 Generate Wiki + Pages (SafeHTML)
      # ------------------------------------------------------------
      - name: 🌈 Generate Wiki + Pages (Glass UI)
        run: |
          set -euxo pipefail
          TAG=${{ steps.tag_from_meta.outputs.version_tag }}
          DATE=$(date '+%Y-%m-%d %H:%M')
          REPO="${{ github.repository }}"
          OWNER="${{ github.repository_owner }}"
          mkdir -p wiki gh-pages

          echo "# 📦 WhispersCpp-Android (${TAG})" > wiki/Downloads.md
          echo "Updated: ${DATE}" >> wiki/Downloads.md
          echo >> wiki/Downloads.md
          echo "## 🧩 Artifacts" >> wiki/Downloads.md
          if compgen -G "artifacts/*.apk" >/dev/null; then
            for f in artifacts/*.apk; do
              N=$(basename "$f")
              echo "- [$N](https://github.com/${REPO}/releases/download/${TAG}/${N})" >> wiki/Downloads.md
            done
          else
            echo "_No artifacts found._" >> wiki/Downloads.md
          fi

          # ✅ Safe HTML: quoted heredoc + placeholder substitution
          cat > gh-pages/index.html <<'EOF'
  <!doctype html>
  <html>
  <head>
  <meta charset="utf-8"/>
  <title>📦 WhispersCpp Downloads</title>
  <style>
  body{font-family:system-ui;background:#111;color:#ddd;text-align:center;padding:40px}
  a{color:#7bdcff;text-decoration:none}
  </style>
  </head>
  <body>
  <h1>📦 WhispersCpp-Android</h1>
<p>Tag: PLACEHOLDER_TAG</p>
  <ul>
  EOF
  
  for f in artifacts/*.apk 2>/dev/null; do
  N=$(basename "$f")
  echo "<li><a href='https://github.com/${REPO}/releases/download/${TAG}/${N}'>$N</a></li>" >> gh-pages/index.html
  done
  
  cat >> gh-pages/index.html <<'EOF'
  </ul>
  <footer>Generated PLACEHOLDER_DATE</footer>
  </body>
  </html>
  EOF
  
  sed -i "s|PLACEHOLDER_TAG|${TAG}|g" gh-pages/index.html
  sed -i "s|PLACEHOLDER_DATE|${DATE}|g" gh-pages/index.html

# ------------------------------------------------------------
# 📘 Push Wiki
# ------------------------------------------------------------
- name: 📘 Push Wiki
  env:
    GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  run: |
    set -euxo pipefail
    REPO="${{ github.repository }}"
    ACTOR="${{ github.actor }}"
    TAG_VAL="${{ steps.tag_from_meta.outputs.version_tag }}"
    WIKI_URL="https://${ACTOR}:${GH_TOKEN}@github.com/${REPO}.wiki.git"
    
    rm -rf wiki.push && cp -r wiki wiki.push
    cd wiki.push
    git init
    git config user.name "$ACTOR"
    git config user.email "$ACTOR@users.noreply.github.com"
    git add .
    git commit -m "📘 Auto-update Wiki (${TAG_VAL})"
    git push --force "$WIKI_URL" master

# ------------------------------------------------------------
# 🚀 Publish gh-pages
# ------------------------------------------------------------
- name: 🚀 Publish gh-pages
  uses: peaceiris/actions-gh-pages@v4
  with:
    github_token: ${{ secrets.GITHUB_TOKEN }}
    publish_dir: gh-pages
    publish_branch: gh-pages
    force_orphan: true
