# ============================================================
# ✅ WhispersCpp-Android — CI Publish (v9.7.5 DebugSync)
# ------------------------------------------------------------
# • Auto-triggered after ci-build.yml success (workflow_run)
# • Also runnable manually (workflow_dispatch)
# • Robust artifact fetch with debug logs + wiki guard + safe tag
# • Fallback: list latest release assets when no artifacts
# ============================================================

name: Android CI Publish (Meta + Pages + Wiki)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to publish (default: main)"
        default: "main"
      use_latest_build:
        description: "Use latest successful build artifacts"
        type: boolean
        default: true
      artifact_name:
        description: "Artifact name uploaded by build workflow"
        default: "release-artifacts"
  workflow_run:
    workflows: ["Android CI Build (Review + Build)"]
    types: [completed]

permissions:
  contents: write

defaults:
  run:
    shell: bash

env:
  EXPECTED_WORKFLOW_NAME: "Android CI Build (Review + Build)"
  DEFAULT_ARTIFACT_NAME: "release-artifacts"

jobs:
  meta_publish:
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') ||
      (github.event_name == 'workflow_dispatch')
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ inputs.branch || 'main' }}

      - name: 🛠️ Install dependencies (jq/unzip)
        run: |
          sudo apt-get update
          sudo apt-get install -y jq unzip

      # ------------------------------------------------------------
      # 🏷️ Safe tag (avoid collisions)
      # ------------------------------------------------------------
      - name: 🏷️ Compute safe tag (avoid collisions)
        id: tag
        run: |
          set -e
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          BASE="v9.7.5-$(date '+%Y%m%d-%H%M')"
          TAG="$BASE"
          n=0
          while git rev-parse "$TAG" >/dev/null 2>&1; do
            n=$((n+1)); TAG="${BASE}-r${n}"
          done

          git tag -a "$TAG" -m "Auto-tag (manual or linked publish)"
          git push origin "$TAG"
          echo "tag_name=$TAG" >> "$GITHUB_OUTPUT"
          echo "🪶 Tagged: $TAG"

      # ------------------------------------------------------------
      # 🔎 Resolve build run id (linked or manual fallback)
      # ------------------------------------------------------------
      - name: 🔎 Resolve build run ID
        id: resolve_run
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          REPO="${{ github.repository }}"
          TARGET_BRANCH="${{ inputs.branch || 'main' }}"
          WF_EXPECTED="${{ env.EXPECTED_WORKFLOW_NAME }}"

          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            RUN_ID="${{ github.event.workflow_run.id }}"
            echo "mode=linked"        >> "$GITHUB_OUTPUT"
            echo "run_id=$RUN_ID"     >> "$GITHUB_OUTPUT"
            echo "🔗 Linked mode: Using build run $RUN_ID"
            exit 0
          fi

          echo "🧭 Manual mode: search latest successful build on branch: $TARGET_BRANCH"
          # 1) try by workflow NAME
          RUN_ID=$(curl -s -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://api.github.com/repos/${REPO}/actions/runs?status=success&branch=${TARGET_BRANCH}&per_page=50" \
            | jq -r --arg name "${WF_EXPECTED}" '.workflow_runs[] | select(.name==$name) | .id' | head -n1)

          if [ -z "$RUN_ID" ] || [ "$RUN_ID" = "null" ]; then
            echo "⚠️ No run by name '${WF_EXPECTED}'. Try any successful run with artifacts…"
            # 2) pick the first run that has at least one artifact
            CANDIDATES=$(curl -s -H "Authorization: Bearer ${GH_TOKEN}" \
              "https://api.github.com/repos/${REPO}/actions/runs?status=success&branch=${TARGET_BRANCH}&per_page=50" \
              | jq -r '.workflow_runs[].id')

            for id in $CANDIDATES; do
              cnt=$(curl -s -H "Authorization: Bearer ${GH_TOKEN}" \
                "https://api.github.com/repos/${REPO}/actions/runs/${id}/artifacts" \
                | jq -r '.artifacts | length')
              if [ "$cnt" != "0" ] && [ "$cnt" != "null" ]; then
                RUN_ID="$id"; break
              fi
            done
          fi

          if [ -z "$RUN_ID" ] || [ "$RUN_ID" = "null" ]; then
            echo "::warning::No valid successful build run found."
            echo "mode=manual"        >> "$GITHUB_OUTPUT"
            echo "run_id="            >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "mode=manual"    >> "$GITHUB_OUTPUT"
          echo "run_id=$RUN_ID" >> "$GITHUB_OUTPUT"
          echo "📦 Selected build run: $RUN_ID"

      # ------------------------------------------------------------
      # ⬇️ Retrieve artifacts (with debug + name fallback)
      # ------------------------------------------------------------
      - name: ⬇️ Retrieve build artifacts (debug)
        id: fetch
        if: ${{ steps.resolve_run.outputs.run_id != '' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          mkdir -p artifacts
          REPO="${{ github.repository }}"
          RUN_ID="${{ steps.resolve_run.outputs.run_id }}"
          NAME_PRI="${{ inputs.artifact_name || env.DEFAULT_ARTIFACT_NAME }}"

          echo "🔎 Listing artifacts in run ${RUN_ID}…"
          curl -s -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://api.github.com/repos/${REPO}/actions/runs/${RUN_ID}/artifacts" \
            | tee artifacts_list.json >/dev/null

          echo "📜 Artifact names in run:"
          jq -r '.artifacts[].name' artifacts_list.json || true

          URL=$(jq -r --arg n "$NAME_PRI" '.artifacts[] | select(.name==$n) | .archive_download_url' artifacts_list.json | head -n1)

          if [ -z "$URL" ] || [ "$URL" = "null" ]; then
            echo "::warning::Primary artifact '$NAME_PRI' not found. Try first artifact in the list."
            URL=$(jq -r '.artifacts[0].archive_download_url' artifacts_list.json)
          fi

          if [ -z "$URL" ] || [ "$URL" = "null" ]; then
            echo "::warning::No downloadable artifact in the run."
            echo "has_artifacts=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "⬇️ Downloading: $URL"
          curl -L -H "Authorization: Bearer ${GH_TOKEN}" "$URL" -o artifacts.zip
          unzip -o artifacts.zip -d artifacts
          rm -f artifacts.zip

          # Show what we got
          echo "📂 Downloaded files:"
          (ls -al artifacts || true)

          if compgen -G "artifacts/*.apk" >/dev/null || compgen -G "artifacts/*.aab" >/dev/null; then
            echo "has_artifacts=true" >> "$GITHUB_OUTPUT"
          else
            echo "::warning::ZIP unpacked but no APK/AAB files present."
            echo "has_artifacts=false" >> "$GITHUB_OUTPUT"
          fi

      # ------------------------------------------------------------
      # ♻️ Optional: Fallback to latest release assets for listing
      # ------------------------------------------------------------
      - name: ♻️ Prepare latest release assets listing (fallback only)
        if: ${{ steps.fetch.outputs.has_artifacts != 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          REPO="${{ github.repository }}"
          echo "🧩 No artifacts this time. Fetch latest release assets just for page listing…"
          curl -s -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://api.github.com/repos/${REPO}/releases/latest" > release.json || true
          if [ -s release.json ]; then
            jq -r '.assets[] | select(.name|test("\\.(apk|aab)$")) | [.name, (.size|tostring), .browser_download_url] | @tsv' release.json \
              > artifacts/_release_assets.tsv || true
            echo "✔️ Fallback listing prepared."
          else
            echo "::warning::No latest release found (or no assets)."
          fi

      # ------------------------------------------------------------
      # 🚀 Release (only when we actually have artifacts)
      # ------------------------------------------------------------
      - name: 🚀 Create/Update GitHub Release
        if: ${{ steps.fetch.outputs.has_artifacts == 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag_name }}
          name: "WhispersCpp Release ${{ steps.tag.outputs.tag_name }}"
          files: |
            artifacts/*.apk
            artifacts/*.aab
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ------------------------------------------------------------
      # 🌈 Generate Wiki + Pages (Glass UI) — shows artifacts or fallback list
      # ------------------------------------------------------------
      - name: 🌈 Generate Wiki + Pages (Glass UI)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          TAG=${{ steps.tag.outputs.tag_name }}
          DATE=$(date '+%Y-%m-%d %H:%M')
          REPO="${{ github.repository }}"
          OWNER="${{ github.repository_owner }}"
          BASE_URL="https://${OWNER}.github.io/$(basename ${REPO})/"
          mkdir -p wiki gh-pages

          # ---------- Wiki ----------
          {
            echo "# 📦 WhispersCpp-Android (${TAG})"
            echo "Updated: ${DATE}"
            echo
            echo "## 🧩 Build Artifacts"
            if compgen -G "artifacts/*.apk" >/dev/null || compgen -G "artifacts/*.aab" >/dev/null; then
              for f in artifacts/*.apk artifacts/*.aab; do
                [ -f "$f" ] || continue
                N=$(basename "$f")
                echo "- [$N](https://github.com/${REPO}/releases/download/${TAG}/${N})"
              done
            elif [ -f artifacts/_release_assets.tsv ]; then
              echo "_Using latest release assets for listing_"
              while IFS=$'\t' read -r N S U; do
                echo "- [$N]($U) — ${S} bytes"
              done < artifacts/_release_assets.tsv
            else
              echo "_No artifacts available for this tag._"
            fi
            echo
            echo "## 🔗 Downloads Page"
            echo "<div align=\"center\">"
            echo "<img src=\"https://api.qrserver.com/v1/create-qr-code/?size=180x180&data=${BASE_URL}\" width=\"180\" height=\"180\"><br>"
            echo "🔗 <a href=\"${BASE_URL}\">Open Glass UI Downloads Page</a>"
            echo "</div>"
          } > wiki/Downloads.md

          # ---------- Pages (Glass UI) ----------
          cat > gh-pages/index.html <<HTML
          <!doctype html>
          <html lang="en"><head><meta charset="utf-8"/>
          <title>📦 WhispersCpp-Android Downloads</title>
          <meta name="viewport" content="width=device-width,initial-scale=1"/>
          <style>
            body{font-family:system-ui;background:#111;color:#ddd;text-align:center;padding:40px;margin:0}
            .box{background:#1b1b1b;border:1px solid #2a2a2a;border-radius:14px;max-width:900px;margin:24px auto;padding:24px}
            a{color:#7bdcff;text-decoration:none}a:hover{text-decoration:underline}
            ul{list-style:none;padding:0} li{margin:8px 0}
            footer{margin-top:20px;color:#aaa}
          </style></head><body>
          <div class="box">
            <h1>📦 WhispersCpp-Android</h1>
            <p>Tag: ${TAG} — Updated: ${DATE}</p>
            <ul>
          HTML

          if compgen -G "artifacts/*.apk" >/dev/null || compgen -G "artifacts/*.aab" >/dev/null; then
            for f in artifacts/*.apk artifacts/*.aab; do
              [ -f "$f" ] || continue
              N=$(basename "$f")
              echo "<li><a href='https://github.com/${REPO}/releases/download/${TAG}/${N}'>$N</a></li>" >> gh-pages/index.html
            done
          elif [ -f artifacts/_release_assets.tsv ]; then
            while IFS=$'\t' read -r N S U; do
              echo "<li><a href='${U}'>$N</a> — ${S} bytes</li>" >> gh-pages/index.html
            done < artifacts/_release_assets.tsv
          else
            echo "<li><em>No artifacts available for this tag.</em></li>" >> gh-pages/index.html
          fi

          cat >> gh-pages/index.html <<'HTML'
            </ul>
            <footer>Generated automatically by CI/CD v9.7.5 DebugSync</footer>
          </div></body></html>
          HTML

      # ------------------------------------------------------------
      # 📘 Push Wiki (guarded)
      # ------------------------------------------------------------
      - name: 📘 Push Wiki (skip if wiki disabled)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          REPO="${{ github.repository }}"
          ACTOR="${{ github.actor }}"
          HAS_WIKI=$(curl -s -H "Authorization: Bearer ${GH_TOKEN}" "https://api.github.com/repos/${REPO}" | jq -r '.has_wiki')
          if [ "$HAS_WIKI" != "true" ]; then
            echo "::warning::Wiki is disabled. Skipping wiki push."
            exit 0
          fi
          WIKI_URL="https://${ACTOR}:${GH_TOKEN}@github.com/${REPO}.wiki.git"
          rm -rf wiki.push && cp -r wiki wiki.push
          cd wiki.push
          git init
          git config user.name "${ACTOR}"
          git config user.email "${ACTOR}@users.noreply.github.com"
          git add Downloads.md
          git commit -m "Auto-update Wiki (v9.7.5 DebugSync)"
          git branch -M master
          git remote add origin "$WIKI_URL"
          git push origin master --force

      # ------------------------------------------------------------
      # 🌐 Publish GitHub Pages (root index.html)
      # ------------------------------------------------------------
      - name: 🚀 Publish gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: gh-pages
          publish_branch: gh-pages
          force_orphan: true
